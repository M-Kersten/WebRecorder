{
  "name": "site-tutorial-video",
  "version": "1.0.0",
  "description": "Turn a flow.json into a narrated, themed tutorial video of a live website.",
  "type": "commonjs",
  "bin": { "site-tutorial-video": "src/index.js" },
  "scripts": {
    "test": "node --test test/*.test.js",
    "demo": "node src/index.js --flow demo/flow.json --theme theme.json --serve demo --no-tts --out out/demo.mp4"
  },
  "dependencies": {
    "playwright": "^1.49.0"
  },
  "license": "MIT",
  "private": true
}
